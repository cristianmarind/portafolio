{
  "name": "portfolio",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@8.19.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "merge": "node scripts/merge-dist.mjs",
    "preview": "npx serve dist -l 3000",
    "deploy": "npm run build && npm run merge"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "turbo": "^2.9.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
